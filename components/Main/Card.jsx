import React from "react";
import { NewsHeaderCard, TaggedContentCard, UserCard } from "react-ui-cards";

import * as St from "./Main.styled";
const Card = () => {
  return (
    <>
      {/* {typeof window !== "undefined" &&
    projectData.map((project) => {
      <St.CardWrapper key={project.id}>
        <TaggedContentCard
          title={project.title}
          description={project.description}
          tags={project.tags}
          thumbnail={project.thumbnail}
        />
      </St.CardWrapper>; */}
    </>
  );
};

export default Card;
