import styled from "styled-components";

const StyledUserDescription = styled.p`
  z-index: 1;
  font-size: clamp(16px, 8vw, 50px);
  grid-row: 5 / span 1;
  grid-column: 2 / span 6;
`;
export default StyledUserDescription;
