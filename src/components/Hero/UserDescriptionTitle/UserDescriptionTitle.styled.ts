import styled from "@emotion/styled";

export const UserDescriptionTitleSt = styled.h1`
  font: var(--title-regular-large);
  font-size: clamp(27px, 8vw, 38px);
  text-align: center;
  letter-spacing: 0.5px;
  word-spacing: 7px;
  opacity: 1;
  scale: 1.2;
  z-index: 1;
`;
